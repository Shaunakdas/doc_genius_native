const initialState = {
  id: null,
  role: null,
  sendbird_id: null,
  channel_url: null,
  graduationYear: null,
};

const currentUser = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default currentUser;
